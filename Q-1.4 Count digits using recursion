#include<stdio.h>
int count(int);
int main()
{
	int n,r;
	printf("Enter any number: ");
	scanf("%d",&n);
	r=count(n);
	printf("%d",r);
	return 0;
}
  int count(int n)
  {
    if (n==0)
    return 0;	
  	else
  	{
	  n=n/10;
  	   return 1+count(n);
    }
}
